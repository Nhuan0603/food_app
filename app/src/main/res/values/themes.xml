<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Food_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Food_app" parent="Base.Theme.Food_app" />

    <!-- Light mode -->
    <!-- Screen background if ligh tmode on -->
    <style name="Screen">
        <item name="android:background">
            #FFF
        </item>
    </style>
    <!-- Text BackGround when light mode on -->
    <style name="TEXT">
        <item name="android:textColor">
            #000
        </item>
    </style>
    <!-- Button when lightmode on-->
    <style name="BUTTON">
        <item name="android:backgroundTint">
            @color/orange_2
        </item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>