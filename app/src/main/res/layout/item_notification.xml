<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="10dp"
    android:background="@color/orange_3">

    <ImageView
        android:id="@+id/img_notification"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_marginEnd="10dp"
        android:src="@drawable/ic_launcher_background"/>
    <TextView
        android:id="@+id/title_notificatoin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/attachment_summary_off"
        android:layout_toEndOf="@+id/img_notification"
        android:textStyle="bold"
        android:textSize="16sp"
        />
    <TextView
        android:id="@+id/content_notification"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/title_notificatoin"
        android:layout_toEndOf="@+id/img_notification"
        android:text="@string/appbar_scrolling_view_behavior"
        android:textColor="@color/white_2"/>
</RelativeLayout>